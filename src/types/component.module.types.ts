export type Item = { label: string; value: string };

export type NewTask = {
  title: string;
  description: string;
  status: string;
  type: string;
  dueDate: string;
  assignee: string;
  urgency: string;
};
